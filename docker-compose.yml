version: '3'

services:
  vpay:
    image: openjdk:17-jdk-slim
    volumes:
      - ./app:/app
    working_dir: /app
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    command: java -jar vpay.jar
    ports:
      - "8899:8899"
